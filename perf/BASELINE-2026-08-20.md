# StockMarketLoop performance project — Phase 1 baseline (2026-08-20)

## Architecture inventory (verified)
- Homepage = custom takeover: WPCode #6865 prints shell loader; `js/home-feed.js` (repo, CDN commit-pinned via #6873 resolver) renders header/tape/rails/feed skin over the real feed markup. Pre-paint guard: WPCode #7202.
- Header/tape/search on OTHER pages: `js/site-search.js`. Loop Bucks button: WPCode #7195 + `js/loop-bucks.js` (+ #7197 milestones). Engagement controls: plugin `feed-engagement-social-bridge/assets/engagement.js` (NOT ours).
- Feed content: server-rendered by the site's feed system; home-feed.js skins + augments (stories, tabs, watchlist, snapshot, groups rails, rabbit-hole "More like this" rail `armRh`).
- Real-time quotes: Render service https://stockmarketloop-loop-kick.onrender.com/api/quotes (poller in home-feed.js + site-search.js header tape).

## Baseline — signed-in desktop (1118×910, warm)
- TTFB 1981 ms · DOMContentLoaded 3393 ms · load 4397 ms
- 84 requests total; **47 wp-json/admin-ajax calls**
- Repeats: **18× sml-home-engagement/v1/counts** (durations up to 15.6 s — server queue collapse), **15× wp/v2/posts** (armRh per-card prefetch), 2× each sml-lb/v1 me|earn|gates|leaderboard, 2× sml-lbm/v1/state
- DOM nodes 1427 · JS transfer 138 KB (13 files) · 31 eager images · Loop Bucks icon 74 KB (repo img/)
- (Earlier audit's 354 KB avatar not reproduced this load; avatar URLs are full-size uploads — fix regardless via Photon resize.)

## Baseline — 375×812 (signed-in)
- documentElement.scrollWidth 375 BUT `#sml-hf-shell` scrollWidth **942 px** → horizontal scrolling inside the fixed shell, blank right region
- Header row overflows: nav links to x=612, Loop Bucks pill to x=744, LOOP-KICK to x=890
- **160** interactive elements under 44×44
- Rails already hidden <1080; no true mobile layout below 768

## Access constraints (stated, not worked around)
- No SSH / WP filesystem; PHP ships via WPCode snippets (hash-verified paste). No Redis control (WP.com Atomic has built-in object cache; server-side caching changes limited to snippet-level transients). Cloudflare zone cache rules not exposed through available tooling → CDN phase documented as recommendations only. DB indexes: no direct SQL access → not attempted.
- Rollback: repo is git (every change one commit, CDN pin moves atomically); WPCode edits are hash-verified with prior content recoverable from repo mirrors in `wpcode/`; WP.com keeps automated backups (Jetpack activity log).

## Planned work (this session), each with kill switch
- P2 mobile layout: scoped `#sml-hf-shell.hfm` rules + classed header markup + bottom nav; disable via `?hfm=0` or localStorage sml_hfm=0.
- P3/P5 request storm: (a) armRh → fetch-on-open (no prefetch storm); (b) client cache+in-flight dedupe for engagement counts (25 s/URL); (c) Loop Bucks in-flight dedupe (2×→1×). Flags: `?hfreq=0` disables the fetch shim.
- P6 images: Photon-resized avatars (?resize=96,96), lazy-load below-fold feed images, recompress Loop Bucks icon.
- P8 polling: quote pollers pause when tab hidden.

## AFTER (deployed 2026-08-20, commits 8abe711..249eb7d + WPCode "SML Home Engagement — counts batch API" hash add5f6e878ed7250)
- Signed-in desktop: **API calls 47 → 9** (engagement counts 18 HTTP → 1 batch POST; post-search prefetch 15 → 0; Loop Bucks reads deduped ×2→×1). Total requests 84 → 33. Engagement rows verified rendering from batch (18 cards, counts + reactions intact), zero console errors.
- Mobile: shell scrollWidth 942→=viewport at **320/390/430** (no horizontal scroll, no blank right region); compact header (logo·search·LOOP-KICK·account), secondary nav + Loop Bucks in fixed bottom bar w/ safe-area padding; 12px gutters; stories scroll in-row; engagement buttons ≥44px (0 under-44 in action rows); 16px inputs (no iOS zoom).
- Images: avatars via Photon ?resize (96px vars, width/height attrs), below-fold card media lazy; loop-bucks icon 75→13.8 KB, logo 45→10.1 KB.
- Polling: homepage quote poll pauses when tab hidden, refreshes on visibility.
- Kill switches VERIFIED live: ?hfm=0 (no mobile layout/class/bottom-nav), ?hfreq=0 (no fetch shim); batch route deactivation → automatic single-call fallback (cbBroken path).

## Not done in this pass (honest gaps)
- TTFB ~1.8–1.9s server-side (WP.com + merged snippet eval) — needs server profiling we lack access to.
- Full sml-home/v2/bootstrap consolidation (LB quad + watchlist + gate + earn remain separate; earn is a deliberate visit-mutation, left untouched).
- DB indexes (no SQL access), Redis policy (platform-managed), Cloudflare cache rules + Brotli/H3 audit (no zone-rule tooling), Render /healthz-/readyz (separate repo not present locally), deploy-time public-page warmer, group-page mobile pass, site-search.js header mobile pass on non-home pages.

## Tranche 2 (2026-08-26, verified in production)
- Render loop-kick: **/healthz** (200, tiny JSON, no deps) + **/readyz** (200; upstream reachability via session route, no secrets) — deployed via autoDeploy, verified live. /api/health was already lightweight JSON (compliant).
- **Public cache warmer** live (WPCode "SML Public Cache Warmer", hash fcf6a1ce9ccc78d3): hourly + on-publish, 11 public URLs (home, section fronts, sitemaps, latest 5 posts) all warmed 200 on first run; options/data pages intentionally excluded (metered providers); status at /wp-json/sml-warm/v1/status; kill = option sml_warmer_off.
- Terminal @390: document scroll clean (docW=390); global header zero overflow. sr-only table + interval wrap fixes shipped.
- Group pages: ship NO viewport meta → phones render scaled-980 desktop (whole Discord layout visible, zoomed). A true <768 layout requires drawer-izing the protected shell — deliberately NOT attempted; documented as its own project.
